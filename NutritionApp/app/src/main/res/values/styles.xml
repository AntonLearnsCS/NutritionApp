<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/acme</item>
        <item name="fontFamily">@font/acme</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
<!-- We need to specifiy android:fontFamily and fontFamily b/c our parent theme uses both-->
    <style name="list_view_button_style">
        <item name="android:layout_width">136dp</item>
        <item name="android:layout_height">41dp</item>
    </style>

  <!--  <style name="Background" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
    </style>-->

    <style name="TextAppearance.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>